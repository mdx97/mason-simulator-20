#include "engine/Mouse.h"
#include "engine/UIComponent.h"

void UIComponent::Update()
{
    /*
    // todo: this will require a system in place to query the object's components by type
    if (SDL_PointInRect(&Mouse::position, rect)) {
        Hover();
        if (Mouse::left_click)
            Click();
    } else {
        Unhover();
    }
    */
}
